---
// Redirección de la raíz "/" al idioma por defecto (Español - mercado principal).
// El redirect real y permanente (301) para producción se define en vercel.json,
// esta página es un respaldo por si Astro sirve el sitio fuera de Vercel.
return Astro.redirect('/es/', 301);
---
